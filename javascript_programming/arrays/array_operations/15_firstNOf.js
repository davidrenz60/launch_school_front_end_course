// Write a function that accepts an array as the first argument and a number as the second.
// Return a new array of elements that go from the first element
// and selects elements up to that count.

function firstNOf(arr, length) {
  return arr.slice(0, length);
}
