// Write a function called lastInArray that receives an array as its argument.
// Within this function, use the length of the array to return the last value in the array.

function lastInArray(array) {
  return array[array.length -1];
}
