// Use the appropriate method on each of these variables to return an integer value of 50.
// Make sure you use each method once.

var a = 50.72;
var b = 49.2;
var c = 49.86;

console.log(Math.ceil(b));
console.log(Math.floor(a));
console.log(Math.round(c));
